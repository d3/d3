#!/usr/bin/env node

var version = 'version:' + JSON.stringify(require("../package.json").version)
  , env = 'env: d3_env'
  , keys = [version, env].join(', ')

console.log("d3 = (function d3_env(d3_window){\n  var d3 = {"+ keys +"};");
