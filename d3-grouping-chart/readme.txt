# D3 Grouping chart

In d3 there exists a simple bar chart where the frequencies are calculated and the sum is displayed as one bar chart. where as this gives the flexibility to display all the frequencies for a particular country to determine which is the best. 

Include `d3.js`, `chart.js` and `chart.css` in your html file, as in `index.html`. 


